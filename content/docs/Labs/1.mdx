---
title: Lab 1
description: Exploratory Data Analysis (EDA) on Titanic Dataset
---

In this practical, we will perform **Exploratory Data Analysis (EDA)** on the Titanic dataset to prepare it for further machine learning tasks.

<div className="fd-steps">
  <div className="fd-step py-2">Import Libraries</div>
  <div className="fd-step py-2">Load Dataset</div>
  <div className="fd-step py-2">Perform Basic EDA</div>
  <div className="fd-step py-2">Handle Missing Values</div>
  <div className="fd-step py-2">Advanced EDA</div>
  <div className="fd-step py-2">Analysis & Insights</div>
  <div className="fd-step py-2">Split Dataset for Model Training</div>
</div>

---

### Import Libraries

```py title="Importing Libraries"
import pandas as pd
import numpy as np
import matplotlib.pyplot as plt
import seaborn as sns
from sklearn.model_selection import train_test_split
from sklearn.preprocessing import StandardScaler
```

**Libraries Used:**

- **pandas (`pd`)** → Data manipulation and analysis
- **numpy (`np`)** → Numerical operations
- **matplotlib** → Basic plotting
- **seaborn** → Advanced data visualization
- **StandardScaler** (from `sklearn.preprocessing`) → Feature scaling
- **train_test_split** (from `sklearn.model_selection`) → Splitting dataset into training and testing sets
